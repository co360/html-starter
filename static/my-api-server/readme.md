A simple REST API server using NodeJS.

We can quickly serve a static `json` file as payload. This can be useful
for local testing.

## How to Run

```bash
npm install
node server.js
open http://localhost:3000/
```
