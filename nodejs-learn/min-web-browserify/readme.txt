A NodeJS application that use "browserify" to package a web app
that's ready for browser use. All the dependencies are "wrapped" into
a self contained "bundle.js".

npm install -g browserify
npm install -g watchify
