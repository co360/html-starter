Explore NodeJS specific here.

## How To Install NodeJS

On MacOSX, run `brew install node` command to install. Usually you also want
to setup `npm` along with `node`.

## How To Setup a NPM and Node based Project

```bash
npm init hello-node
cd hello-node
npm install
echo 'console.log("Hello");' > hello.js
node hello.js
```
